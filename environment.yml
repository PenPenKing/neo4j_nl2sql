# 使用: conda env create -f environment.yml
# 激活: conda activate neo4j_nl2sql
name: neo4j_nl2sql
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - -r requirements.txt
